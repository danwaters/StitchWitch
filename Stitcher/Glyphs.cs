using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Stitcher
{
    public class Glyphs
    {
        public static List<string> AvailableGlyphs = new List<string>
            {
                
                "&minus;",
                "&dagger;",
                "&omicron;",
                "&bull;",
                
                "&iota;",
                
                "&nabla;",
                "&lowast;",

                /*
                "&Zeta;",
                "&Nu;",
                "&Beta;",
                "&cap;",
                "&cup;",
                "&and;",
                "&or;",
                "&loz;",
                "&diams;"
                 * */
            };
    }
}
